import React, { FC } from "react";
import { CreateFilterOptionsConfig } from "@mui/material";
import { ITableProps } from "types/components";

const Table: FC<ITableProps> = ({ renderItems, items }) => {
  return (
    <>
      {/* Look at components > base > Select for a sample on how to handle items*/}
    </>
  );
};

export default Table;
